#include <iostream>
#include <string>
#include <ctime>

/*
int main()
{
    // auto type assign, maybe not use it unless very sure
    // auto x = 10.5;

    int x = 10;
    if(x > 6)
    {
        std::cout << "X is greater than 6" << std::endl;
    }
    if(0)
    {
        std::cout << "this will never be printed" << std::endl;
    }

    if (x > 2)
        std::cout << "OK, x > 2" << std::endl;

    int age;
    std::cout << "Enter person's age: ";
    std::cin >> age;

    if(age >= 18)
    {
        std::cout << "Person is an adult!" << std::endl;
    }else if(age > 13)
    {
        std::cout << "Person is a teenage!" << std::endl;
    }else if(age > 3)
    {
        std::cout << "Person is a kid!" << std::endl;
    }else{
        std::cout << "Person is a child!" << std::endl;
    }

    return 0;
}
*/
